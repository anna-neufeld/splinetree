# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: false
cache: packages

r_check_args: "--no-vignettes"


script:
  - |
    R CMD build --no-build-vignettes .
    travis_wait 40 R CMD check splinetree*tar.gz